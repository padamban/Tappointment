import { all } from './all.environment';

export const environment = {
  production: false,
  regAccessKey: all.regAccessKey,
  regAccessKeyPrefil: all.regAccessKey,
  version: all.version,
  firebase: all.firebase
};
